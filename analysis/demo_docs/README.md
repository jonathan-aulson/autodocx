# Demo Docs Refresh

`python scripts/render_relationship_demo.py` now generates example business components that include the new scaffold hints (process dependencies, datastore coverage, and identifier lists). Re-run the script after any extractor change to verify the Markdown under `analysis/demo_docs/components/**` includes populated **Identifiers** and **Dependencies** sections.
